EXAMPLE EXECUTABLE FILE

TO EXECUTE MULTIROUND EXAMPLE:
	$cd multiRound
	$make
	$./exec.byte -i mr_data.txt -o multiRound.txt -cl 1 -rw 10 -r 2 -n 10 -k 10

   #ARGUMENTS:
     -rw specify the rows of the database, -rw int
     -cl specify the cols of the database, -cl int
     -r specify the rounds of the experiments, -r int
     -i specify the input file name, -i string
     -o specify the output file name, -o string
     -n specify the argument n, -n int
     -k specify the argument k, -k int
     -help  Display this list of options


TO EXECUTE TWOROUND EXAMPLE:
	$cd twoRound
	$make
	$./exec.byte -i tr_data.txt -o twoRound.txt -cl 10 -rw 10 -r 2

   #ARGUMENTS:
     -rw specify the rows of the database, -rw int
     -cl specify the cols of the database, -cl int
     -r specify the rounds of the experiments, -r int
     -i specify the input file name, -i string
     -o specify the output file name, -o string
     -help  Display this list of options