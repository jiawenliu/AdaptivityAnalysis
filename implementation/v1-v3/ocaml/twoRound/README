EXAMPLE EXECUTABLE FILE

TO EXECUTE TWOROUND EXAMPLE:
	$make
	$./driver -o results/mechname.txt -mech mechname -rw 1000 -r 1000 -clst 5 -cl 100 --createdb

   #ARGUMENTS:
     -rw specify the rows of the database, -rw int
     -cl specify the end point of the range of the database colnums, -cl int
     -clst specify the begin point of the range of database colnums, -clst int
     -r specify the rounds of the experiments, -r int
     -o specify the output file name, -o string 
     -mech specify the mechanism you want to use -mech mechname
     --createdb create new database by uniform distribution
     -help  Display this list of options