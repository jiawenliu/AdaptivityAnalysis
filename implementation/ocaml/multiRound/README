EXAMPLE EXECUTABLE FILE

TO EXECUTE MULTIROUND EXAMPLE:
	$cd multiRound
	$make
	$./driver --createdb -rw 10 -r 10 -t 1 -o results/N100n10.txt -mech non -N 100

   #ARGUMENTS:
      --createdb: create a new db;
      -rw: specify the size (row number) of the database, -rw float; 
      -r: specify the rounds (parameter k) of the multiRounds algorithm
      -t: specify the trails of the experiments, -t int;
      -o: specify the output file name, -o string;
      -mech: specify the mechanism name, -mech string;
      -N: specify the size of the population, -N int



