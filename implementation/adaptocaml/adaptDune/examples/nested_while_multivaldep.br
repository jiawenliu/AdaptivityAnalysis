[ x <- query ( 0 ) ] 1 ;
[ z <- query ( 1 ) ] 2 ;
while [  > (k , 0) ] 3 do {
[ k <- - (k, 1) ]4;
[ z <- query ( + (x, z) ) ] 5;
[ i <- k ]6
while [  > (i , 0) ] 7 do 
{ [ i <- - (i, 1) ]8;
[ x <- query ( + (x, z) ) ] 9 }
 }