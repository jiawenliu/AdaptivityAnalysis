let g = 
fix round (j). lambda db. lambda k.
	if((j < k), 
	let a = mech (lambda x. (get x j) * (get x k)) 
	in
	(a,j)::(round (j + 1) db k)
	, nil)
in
fix twoRound(k). lambda db. 
 let l = g 0 db k
	in let q = lambda x. sign(foldl (lambda acc. lambda (a,i). (acc + (get x i) * log( (1 + a)/ (1 - a) ) ) ) 0 l  )
		in mech(q)



|- real