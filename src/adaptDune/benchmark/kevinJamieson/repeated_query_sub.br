    [ l <- 0 ] 0 ;
    [ queried_set <- query(chi : 0 :) ] 1 ;
    [a <- -1] 2 ;
while [a < 0] 3 do
{   [delta_l <-  / ( (*  ((+ (l, 1) ), log( / (2 , delta) ))) , (** (2,l))) ] 4;
    [k <- ** ( 2 , l ) ];
    while [k > 0] do
    {
        [ r <- query(chi : queried_set : ) ];
        if [r > 0]
        then {
            [queried_set <- + (queried_set, r)]
        }
        else
        {
            [k <- 0]
        }
    }
    [ l <- l + 1 ];
    [a <-  - (queried_set, delta_l)]
}